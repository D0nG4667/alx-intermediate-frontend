# Steps to install SASS using pnpm with the latest Node.js version

# 1. Install Node Version Manager (nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
exit

# 2. Reopen the terminal and install the latest Node.js version
nvm install node
nvm use node

# 3. Confirm installation
node -v
npm -v

# 4. Initialize pnpm if not already installed
npm install -g pnpm

# 5. Navigate to the project directory
cd alx-intermediate-frontend/0x03-sass_scss

# 6. Install latest SASS version using pnpm
pnpm add sass --save-dev

# 7. Verify installation
pnpm sass --version
